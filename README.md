# Notes API (Go)

Simple REST API built using Go standard library.

## Features

- GET /notes → get all notes  
- GET /notes?id=1 → get single note  
- POST /notes → create note  
- DELETE /notes?id=1 → delete note  

## Run

```bash
go run main.go